set -e

. common/setup-java-prepackaging.sh


cd ${BUILD_DIR}/elpaaso-sandbox-service
mvn -B -e clean install -DskipTests=true
mv target/elpaaso-sandbox-service-0.0.1-SNAPSHOT.jar  ${BUILD_DIR}/elpaaso-sandbox-service.jar


cd ${BUILD_DIR}
rm -rf elpaaso-sandbox-service

cleanup_java

