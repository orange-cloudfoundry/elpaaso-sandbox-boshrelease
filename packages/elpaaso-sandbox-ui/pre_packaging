set -e

. common/setup-java-prepackaging.sh


cd ${BUILD_DIR}/elpaaso-sandbox-ui
mvn -B -e clean install -DskipTests=true
mv target/elpaaso-sandbox-ui-0.0.1-SNAPSHOT.jar  ${BUILD_DIR}/elpaaso-sandbox-ui.jar


cd ${BUILD_DIR}
rm -rf elpaaso-sandbox-ui

cleanup_java

